import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Flutter-Telephony-Client',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
